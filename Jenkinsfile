pipeline {
    agent any
    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello Jenkins configuration from GitHub with Webhook - first run done(4)!'
            }
        }
    }
}
